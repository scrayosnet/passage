apiVersion: v1
kind: Service
metadata:
  name: {{ include "passage.fullname" . }}
  labels:
    {{- include "passage.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "passage.selectorLabels" . | nindent 4 }}
