TBD

() -> discovery -> filter[] -> strategy -> ()
() -> selection -> layer[] --> (first) --> ()

Replace strategy with sorting filter? (always select first) -> sort layers only sort with the same priority

Add cache for all? User-specific translations?

Maybe pass filter, sort, and strategy to the adapter using some template (see rig vector store)?

Player block/allowlists have to be queried before agones allocation!

Translate (enum) filters into selector-specific filters?

---

- Fixed + Filter/Sort
- DNS (cached) + Filter/Sort
- Agones with Filter/Sort
- gRPC (with Filter/Sort)
