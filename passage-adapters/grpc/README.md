# gRPC Adapters

This crate provides gRPC implementations for all adapters. Each adapter is implemented as a gRPC client that sends the request to an external gRPC service implementing the interface defined in `./proto/adapter`.
