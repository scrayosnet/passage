# See settings documantation at src/settings.rs.

address = "0.0.0.0:25565"
timeout = 120

[sentry]
enabled = false
debug = false
address = "https://key@sentry.io/42" # update if enabled
environment = "staging"

[metrics]
enabled = false
auth_enabled = false
username = "username" # update if (auth) enabled
password = "password" # update if (auth) enabled

[rate_limiter]
enabled = true
duration = 60
size = 60

[resourcepack]
adapter = "none"

[status]
adapter = "none"

[target]
adapter = "none"

[target.fixed]
target = "116.202.130.184:26426"

[target_strategy]
adapter = "none"
